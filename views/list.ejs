<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>자유 게시판</title>
  <link rel="stylesheet" href="/board.css">
</head>
<body>
  <div class="container">
    <h1>자유 게시판</h1>

    <div class="header-actions">
      <form class="search-form" method="GET" action="/">
        <input type="text" name="search" placeholder="제목으로 검색..." value="<%= search %>">
        <button type="submit" class="btn btn-primary">검색</button>
        <% if (search) { %>
          <a href="/" class="btn btn-primary">전체보기</a>
        <% } %>
      </form>
      <a href="/write" class="btn btn-success">글쓰기</a>
    </div>

    <% if (search) { %>
      <p class="info-text">검색어 '<%= search %>'에 대한 결과: <%= total %>건</p>
    <% } else { %>
      <p class="info-text">전체 게시물: <%= total %>건</p>
    <% } %>

    <% if (posts.length > 0) { %>
      <table class="post-table">
        <thead>
          <tr>
            <th class="col-num">번호</th>
            <th class="col-title">제목</th>
            <th class="col-author">작성자</th>
            <th class="col-date">작성일</th>
            <th class="col-views">조회수</th>
          </tr>
        </thead>
        <tbody>
          <% posts.forEach(post => { %>
            <tr>
              <td class="col-num"><%= post.id %></td>
              <td class="col-title">
                <% if (post.is_notice) { %>
                  <span class="notice-badge">공지</span>
                <% } %>
                <a href="/posts/<%= post.id %>" class="post-title"><%= post.title %></a>
              </td>
              <td class="col-author"><%= post.author %></td>
              <td class="col-date"><%= new Date(post.created_at).toLocaleString('ko-KR') %></td>
              <td class="col-views"><%= post.views %></td>
            </tr>
          <% }); %>
        </tbody>
      </table>

      <div class="pagination">
        <% if (currentPage > 1) { %>
          <a href="/?page=<%= currentPage - 1 %><%= search ? '&search=' + search : '' %>">&laquo; 이전</a>
        <% } %>

        <%
          const maxPages = 10;
          let startPage = Math.max(1, currentPage - Math.floor(maxPages / 2));
          let endPage = Math.min(totalPages, startPage + maxPages - 1);

          if (endPage - startPage + 1 < maxPages) {
            startPage = Math.max(1, endPage - maxPages + 1);
          }

          for (let i = startPage; i <= endPage; i++) {
        %>
          <% if (i === currentPage) { %>
            <span class="active"><%= i %></span>
          <% } else { %>
            <a href="/?page=<%= i %><%= search ? '&search=' + search : '' %>"><%= i %></a>
          <% } %>
        <% } %>

        <% if (currentPage < totalPages) { %>
          <a href="/?page=<%= currentPage + 1 %><%= search ? '&search=' + search : '' %>">다음 &raquo;</a>
        <% } %>
      </div>
    <% } else { %>
      <div class="no-posts">
        <p>게시물이 없습니다.</p>
      </div>
    <% } %>
  </div>
</body>
</html>
